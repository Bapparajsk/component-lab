/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.aceternity.com", "api.microlink.io", "www.thewowstyle.com", "extension.harvard.edu", "img.icons8.com", "seeklogo.com"],
  }
};

export default nextConfig;
